# MoonCollections

A data structure library for MoonBit.

- **IndexMap** — order-preserving hash map with `HashMap` + `Array` backing
- **BitSet** — fixed-size bit-level data structure with set operations

See [README.md](README.md) for full documentation.